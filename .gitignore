venv/
__pycache__/
*.pyc
*.db
.env
.worktrees/

# Local Claude Code state (per-machine)
.claude/

# macOS metadata
.DS_Store

# Runtime user state — written by the app at runtime
ucat_settings.json
ucat_telemetry.jsonl

# Render-test outputs — regenerated by tests, not meant for source control
assets/test_renders/
